# Variables expuestas al browser por Vite (prefijo VITE_).

# URL del backend de QA Forge. En prod (Vercel): apuntar al backend en Railway.
VITE_API_URL=http://localhost:3001

# Opcional — solo si el socket vive en otro host. Por default usa VITE_API_URL.
# VITE_SOCKET_URL=http://localhost:3001
